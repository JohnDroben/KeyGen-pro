# 🔐 Генератор паролей с шифрованием

Современное приложение для генерации и безопасного хранения паролей с графическим интерфейсом.

## 🌟 Особенности

- Генерация криптостойких паролей
- Тёмная тема с синими акцентами
- Настройка длины и набора символов
- Проверка сложности пароля
- Копирование в буфер обмена
- **Шифрование** всех сохранённых паролей
- Экспорт в `.txt` (с защитой мастер-паролем)
- Удаление всей истории
- Простой и удобный интерфейс

## 🔐 Безопасность

- Пароли хранятся в зашифрованном виде (`Fernet` + `PBKDF2`)
- Ключ шифрования выводится из **мастер-пароля** (не хранится!)
- Соль используется для защиты от атак по таблицам
- Экспорт возможен только после ввода мастер-пароля

> ⚠️ **Внимание**: Это приложение не предназначено для хранения критически важных данных в производственной среде. Используйте на свой страх и риск.

## 📦 Установка

1. Убедитесь, что установлен Python 3.8+
2. Установите зависимости:

```bash
pip install -r requirements.txt



## 🔐 "Где найти ключ шифрования, выведенный из мастер-пароля?"*

❓ Краткий ответ:
Ключ шифрования нигде не хранится — он временно создаётся каждый раз на основе мастер-пароля и соли.

🔍 Подробно: как это работает
Когда вы вводите мастер-пароль, приложение делает следующее:

1. Соль (salt) — salt.bin
Хранится в папке data/salt.bin
Генерируется один раз при первом сохранении пароля
Нужна, чтобы защититься от rainbow-таблиц
❗ Не является секретом, но должна быть уникальной
2. Вывод ключа (key derivation) — PBKDF2HMAC
Из мастер-пароля и соли с помощью алгоритма PBKDF2 создаётся криптостойкий ключ
Чем сложнее мастер-пароль — тем безопаснее ключ
python
kdf = PBKDF2HMAC(
    algorithm=hashes.SHA256(),
    length=32,              # 32 байта = 256 бит (для Fernet)
    salt=salt,              # из salt.bin
    iterations=100000       # замедление для защиты от брутфорса
)
key = base64.urlsafe_b64encode(kdf.derive(master_password.encode()))
3. Ключ используется для Fernet
Полученный key передаётся в Fernet — криптографический инструмент
Fernet шифрует и расшифровывает данные
python
fernet = Fernet(key)
encrypted_data = fernet.encrypt(b"ваш пароль")
🗃️ Где что находится на диске?
| Файл / Папка | Путь | Назначение | |---------------------------|--------------------------|-----------| | salt.bin | data/salt.bin | Соль для вывода ключа (хранится) | | saved_passwords.enc | data/saved_passwords.enc | Зашифрованные пароли | | Мастер-пароль | — | ❌ Нигде не хранится! Только вводится вручную | | Ключ шифрования (Fernet key) | — | ❌ Никогда не сохраняется. Генерируется временно при каждом вводе мастер-пароля |

⚠️ Что будет, если забыть мастер-пароль?
Восстановить пароли — невозможно.

Потому что:

Без правильного мастер-пароля ключ не совпадёт
Fernet выдаст ошибку расшифровки
Данные останутся навсегда зашифрованными
Это как потерять ключ от сейфа — сам сейф на месте, но открыть его нельзя.

✅ Пример: попытка расшифровать
python
# При вводе мастер-пароля "123"
key1 = derive_key("123", salt)  # → ABC... (неправильный)

# При вводе правильного мастер-пароля "mystrongpass"
key2 = derive_key("mystrongpass", salt)  # → XYZ... (правильный)

# Только key2 сможет расшифровать saved_passwords.enc
🛡️ Рекомендации
Запишите мастер-пароль в надёжное место (например, на бумагу или в доверенный менеджер паролей).
Не используйте простые пароли — PBKDF2 защищает, но брутфорс всё ещё возможен.
Храните salt.bin и saved_passwords.enc вместе — без соли расшифровать нельзя.
🧩 Проверка: как убедиться, что всё работает?
Запустите приложение
Сгенерируйте пароль → нажмите "Сохранить" → введите мастер-пароль (например, qwerty)
Закройте программу
Перезапустите
Нажмите "Просмотреть сохранённые" → введите тот же мастер-пароль → пароль появится

Введите другой пароль → увидите: ❌ Неверный мастер-пароль
